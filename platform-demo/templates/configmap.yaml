apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "platform-demo.fullname" . }}-config
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "platform-demo.labels" . | nindent 4 }}
data:
  APP_MESSAGE: {{ .Values.config.appMessage | quote }}

